name: Hello World

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello world
        run: echo "Hello world"

      - name: print DevOps is fun
        run: echo "DevOps is fun"
